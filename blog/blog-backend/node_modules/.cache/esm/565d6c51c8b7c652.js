let jwt;_c8b‍.x([["default",()=>_c8b‍.o]]);_c8b‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


const jwtMiddleware = (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) return next(); // 토큰이 없음
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    ctx.state.user = {
        _id: decoded._id,
        username: decoded.username,
    };
    _c8b‍.g.console.log(decoded);
    return next();
} catch (e) {
    // 토큰 검증 실패
    return next();
  }
};



_c8b‍.d(jwtMiddleware);